<%= render "/layouts/header", heading:'Edit Card', subText: false, size: 'small' %>

  <%= render 'form' %>

      <%= button_to 'Destroy', [@bar,@tap], method: :delete, data: { confirm: 'Are you sure?' }, class: "button red" %>
