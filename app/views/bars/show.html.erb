<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @bar.title %>
</p>

<% if @bar.user_id == current_user %>
<%= link_to 'Edit', edit_bar_path(@bar) %>
<% end %>
<%= link_to 'Back', bars_path %>
