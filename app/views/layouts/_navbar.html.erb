<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= image_tag("5.png", size: "48x40", alt: "Logo", class:"clear-top") %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
        <li><p class="navbar-text hello">Hello OnTap User!!</p></li>

        <!-- <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin Actions <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if current_user.admin  %>
            <li><%= link_to "Create New User", new_user_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Review API Keys", apikeys_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Edit User Access", users_path %></li>
            <% end  %>
          </ul>
        </li> -->
        <li><%= link_to "Create Bar", new_bar_path %></li>
        <li><%= link_to "All Bars", bars_path %></li>
        <li><%= link_to "All Taps", taps_path %></li>
        <li><%= link_to "Edit Creds", edit_user_path(current_user) %></li>
      </ul>

          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Sign Out", sessions_path, method: :delete %></li>
          </ul>
        <% end %>


      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if !user_signed_in? %>
        <li><%= link_to "Sign In", new_session_path %></li>
        <li><%= link_to "Register", new_user_path %></li>

        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
